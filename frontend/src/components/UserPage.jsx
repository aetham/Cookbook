import React, { Component } from "react";
import { Container} from 'react-bootstrap'
export default class UserPage extends Component {
    // Mapping the stored data onto React Card components.
    render() {
        return (
            <div>
            </div>
        );
    }
}